@page
@model Thesis.Pages.Degrees.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Degree[0].DegNameAr)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Degree[0].DegNameEn)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Degree) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DegNameAr)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DegNameEn)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DegId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DegId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DegId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
